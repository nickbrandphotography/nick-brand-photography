Drop your Team photos in this folder, then run:

    npm run ingest

Full-size JPEGs straight out of your editor are fine — the script resizes,
optimises and renames them. Your originals are never modified, and re-running
the command is safe (it skips anything already processed).

See scripts/README-images.md for the details.
